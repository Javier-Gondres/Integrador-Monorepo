# Copia a apps/web/.env.development (Next lo carga en `next dev`).
# No subas el archivo real a Git.

# URL del API (Nest en local suele ser http://localhost:3001)
NEXT_PUBLIC_API_URL=http://localhost:3001
# Solo si usas Prisma u otra conexión desde Next en local:
# DATABASE_URL="postgresql://..."
