{
  "name": "integrador-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "build:api": "turbo run build --filter=api",
    "dev": "dotenv -e packages/database/.env.development -- turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,md}\"",
    "code:check": "pnpm format && pnpm format:check && pnpm lint && pnpm check-types",
    "check-types": "turbo run check-types",
    "db:migrate": "pnpm --filter @repo/db run db:migrate",
    "db:generate": "pnpm --filter @repo/db run db:generate",
    "db:deploy": "pnpm --filter @repo/db run db:deploy",
    "db:seed:dev": "pnpm --filter @repo/db run db:seed:dev",
    "db:seed:staging": "pnpm --filter @repo/db run db:seed:staging",
    "db:seed:production": "pnpm --filter @repo/db run db:seed:production",
    "db:seed:production:bootstrap": "pnpm --filter @repo/db run db:seed:production:bootstrap",
    "db:deploy:staging": "pnpm --filter @repo/db run db:deploy:staging",
    "db:deploy:production": "pnpm --filter @repo/db run db:deploy:production",
    "dev:web:staging": "pnpm --filter web run dev:staging",
    "dev:api:staging": "pnpm --filter api run dev:staging",
    "dev:staging": "dotenv -e packages/database/.env.development -- turbo run dev:staging"
  },
  "devDependencies": {
    "@napi-rs/cli": "^3.6.2",
    "dotenv-cli": "^8.0.0",
    "lightningcss-win32-x64-msvc": "^1.32.0",
    "prettier": "^3.7.4",
    "turbo": "^2.9.10",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": "24.x"
  },
  "pnpm": {
    "overrides": {
      "@types/node": "^24.0.0"
    }
  }
}
