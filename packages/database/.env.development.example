# Copia a packages/database/.env.development — usado por `pnpm db:generate`, `db:migrate`, `db:deploy` (Docker / local).
# Sin pooler: misma URL en ambas.

DATABASE_URL="postgresql://postgres:postgres@127.0.0.1:5433/integrador?schema=public&sslmode=disable"
DIRECT_URL="postgresql://postgres:postgres@127.0.0.1:5433/integrador?schema=public&sslmode=disable"
